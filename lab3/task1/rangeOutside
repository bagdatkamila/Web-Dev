<!DOCTYPE html>
<html>
    <body>
        <script>
            let age = prompt('');
            if(!(age >= 14 && age <= 90)){
                alert("Young enough");
            }else{
                alert("You are too old or you are baby");
            }
            // if(age < 14 || age > 90)
        </script>
    </body>
</html>